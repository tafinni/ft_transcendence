// Server library, contains functions and variables
// use keyword export for everything accessed from outside

export var libvar = 42

export function libtest(x) {
    //if (x === 'true')
    //  return true
    //return false
    return (x === 'true')
}